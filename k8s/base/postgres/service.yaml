apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: batchsender
  labels:
    app: postgres
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  selector:
    app: postgres
