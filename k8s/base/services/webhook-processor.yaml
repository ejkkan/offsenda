apiVersion: apps/v1
kind: Deployment
metadata:
  name: webhook-processor
  namespace: batchsender
  labels:
    app: webhook-processor
    component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webhook-processor
  template:
    metadata:
      labels:
        app: webhook-processor
        component: worker
    spec:
      containers:
      - name: webhook-processor
        image: ghcr.io/batchsender/worker:latest
        command: ["node", "dist/webhook-processor.js"]
        ports:
        - containerPort: 6001
          name: metrics
        envFrom:
        - configMapRef:
            name: worker-config
        - secretRef:
            name: worker-secrets
        env:
        - name: PORT
          value: "6001"
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 6001
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 6001
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: webhook-processor
  namespace: batchsender
spec:
  selector:
    app: webhook-processor
  ports:
  - port: 6001
    targetPort: 6001
    name: metrics
