# Traefik BasicAuth Middleware for Prometheus
# Uses the prometheus-basic-auth secret (sealed in sealed-prometheus-auth.yaml)
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: prometheus-auth
  namespace: batchsender
spec:
  basicAuth:
    secret: prometheus-basic-auth
