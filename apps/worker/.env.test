DATABASE_URL="postgresql://batchsender:batchsender@localhost:5433/batchsender"
WEBHOOK_SECRET="test-secret"
DRAGONFLY_URL="localhost:6379"
NATS_CLUSTER="nats://localhost:4222"
CLICKHOUSE_URL="http://localhost:8123"
CLICKHOUSE_USER="default"
CLICKHOUSE_PASSWORD="clickhouse"
CLICKHOUSE_DATABASE="batchsender"
EMAIL_PROVIDER="mock"
NODE_ENV="test"

# IMPORTANT: Disable rate limiting in tests to avoid requiring Dragonfly/Redis
# This allows tests to run fast without external dependencies
DISABLE_RATE_LIMIT=true

# Provider rate limits (messages per second)
# These can be overridden per test scenario when rate limiting is enabled
SES_RATE_LIMIT=14
RESEND_RATE_LIMIT=100
MOCK_RATE_LIMIT=1000
