/**
 * Worker Configuration
 *
 * Re-exports from @batchsender/config.
 * All config is centralized in the shared package.
 */

export { config, loadConfig, resetConfig, configSchema } from "@batchsender/config";
export type { Config } from "@batchsender/config";
